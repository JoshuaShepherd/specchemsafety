{
  "$schema": "https://json.schemastore.org/mcp.json",
  "description": "Model Context Protocol configuration for SpecChem Safety LMS",
  "mcpServers": {
    "gitkraken": {
      "command": "gk",
      "args": ["mcp", "server"],
      "description": "GitKraken MCP provides Git operations with AI assistance",
      "enabled": true
    }
  }
}