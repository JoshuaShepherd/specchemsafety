{
  "name": "specchem-safety-lms",
  "version": "1.0.0",
  "description": "SpecChem Safety Learning Management System",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/safety-lms && npm run build",
    "dev": "cd apps/safety-lms && npm run dev",
    "start": "cd apps/safety-lms && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}