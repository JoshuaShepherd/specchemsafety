{
  "name": "specchem-safety-lms",
  "version": "1.0.0",
  "description": "SpecChem Safety Learning Management System",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter @specchem/safety build",
    "dev": "pnpm --filter @specchem/safety dev",
    "start": "pnpm --filter @specchem/safety start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}